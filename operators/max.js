function max(a, b) {
    /**
     * Finds the larger of the two numbers. (TODO: Implement this)
     */
    // --- YOUR CODE GOES HERE - MAKE THE FUNCTION RETURN THE RESULT WITH `return` ---
    return a > b ? a : b;
}

export default max;