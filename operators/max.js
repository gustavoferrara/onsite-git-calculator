function max(a, b) {
  /**
   * Finds the larger of the two numbers. (TODO: Implement this)
   */
  // Return the greater value (if equal, returns `a`)
  return a >= b ? a : b;
}

export default max;
